FROM python:3.12-slim

RUN pip install --no-cache-dir fastapi uvicorn[standard]

WORKDIR /app
COPY fraud_mock ./fraud_mock

CMD ["uvicorn", "fraud_mock.main:app", "--host", "0.0.0.0", "--port", "8010"]
