[project]
name = "demo-payment-service"
version = "0.1.0"
description = "Demo FastAPI payment service with injectable bugs"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.7",
    "pytest>=8.3",
]

[tool.ruff]
line-length = 100
target-version = "py312"
